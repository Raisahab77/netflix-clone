import { CommaSepratedListPipe } from './comma-seprated-list.pipe';

describe('CommaSepratedListPipe', () => {
  it('create an instance', () => {
    const pipe = new CommaSepratedListPipe();
    expect(pipe).toBeTruthy();
  });
});
